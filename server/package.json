{
  "name": "julesops-server",
  "version": "0.2.0",
  "private": true,
  "description": "Experimental JulesOps backend and dashboard prototype (parked)",
  "scripts": {
    "start": "node src/server.js",
    "smoke": "node scripts/smoke-test.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --require ./src/__tests__/setup.js --test src/__tests__/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "license": "MIT"
}
